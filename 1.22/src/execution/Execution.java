package execution;

import processing.Processing; // 処理用のクラスをインポート

public class Execution {
    public static void main(String[] args) {
        Processing processing = new Processing(); // Processingクラスのインスタンスを作成
        processing.printMessages(); // メッセージを出力するメソッドを呼び出す
    }
}
